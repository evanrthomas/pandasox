package server;
import server.Card;
import server.Affinity;

public class Deck {
  ArrayList<Card> cards;
  public Deck(ArrayList<Card> cards) {
    this.cards = cards;
  }

  public static Deck getStartingDeck() {
    for (Card s: ["Move", "Swap", "Gem"]) {
      Card.
    };

  }

}
